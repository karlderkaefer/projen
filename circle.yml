# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

version: "2.1"
orbs:
  cdk: signavio/cdk-orb@0.10.8
workflows:
  build:
    jobs:
      - cdk/test:
          context:
            - NPM
      - cdk/sonarqube:
          requires:
            - cdk/test
          context:
            - NPM
            - Sonarqube
      - cdk/publish:
          requires:
            - cdk/test
          context:
            - npm-github
          filters:
            - branches:
                only:
                  - master
                  - main
